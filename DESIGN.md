# DESIGN — Linux Cockpit 界面设计规范

> 面向本项目（Electron + Vue 3 + Vuetify 3，深色 Material 3 主题）的 UI 设计约定。
> 本文解决一个反复出现的问题：**界面设计得"太紧"**——元素贴边、文字挤顶部、间距不足。
> 新写/新改 UI 前先读一遍，尤其注意「§3 尺寸底线」与「§4 常见误区」。

---

## 1. 核心原则

**1.1 间距是呼吸，不是浪费。**
面板、卡片、工具栏的留白让信息分层可读。默认宁可偏松，不可偏紧——紧凑感是最后一步微调，不是起点。

**1.2 别用小尺寸逃避布局问题。**
遇到"放不下"先想"是不是该换行/分组/缩层级"，而不是把字号和 padding 一路调小。小字号 + 小 padding = 文字贴边、可读性崩坏。

**1.3 控件尺寸跟着用途走。**
- **文字按钮**（停止 / 移除 / 发送 / 导出）→ 默认密度，文字要有呼吸。
- **图标按钮**（自动滚动 / 清空 / 关闭）→ 允许紧凑（`size="small"`），图标本就不需要大内边距。
- **chip** → 默认密度 + 显式 `padding-block`，否则 label 贴 chip 顶/底。

**1.4 状态是设计的一部分。**
运行时变化（任务结束、错误、进度完成）要反映在 UI 上——比如任务结束后的计时应**冻结**，而不是继续用当前时钟算。数据模型要有结束时间，界面才能表现"已结束"。

---

## 2. 参考实现：apps 能力（本项目的排版标杆）

`src/abilities/apps/View.vue` 是现有最成熟的布局，新 UI 应模仿它：

```vue
<!-- 卡片操作区 -->
<v-card-actions class="px-4 pb-4 pt-0 ga-2">
  <v-btn variant="text" prepend-icon="mdi-pencil">编辑</v-btn>
  <v-spacer />
  <div class="d-flex ga-2 justify-end flex-wrap">...</div>
</v-card-actions>
```

- 按钮带图标 + 文字（比纯文字更易扫读），`ga-2`（8px）间距
- `flex-wrap`：空间不足时换行，不压缩按钮
- 操作分组：次要动作（编辑）与主要动作（启动/操作）用 `v-spacer` 分开

---

## 3. 尺寸底线（不要低于这些）

| 元素 | 最小规范 | 原因 |
|---|---|---|
| 文字按钮 `v-btn` | 默认密度（不设 `size="small"`） | 默认高度下文字不贴顶 |
| chip 内边距 | `padding-block: 4px` + `min-height: 24px` | 否则 label 贴 chip 边框 |
| 工具栏行距 | `py-3` / `pb-3` 起步 | 避免按钮紧贴分隔线/卡片顶 |
| 区块间距 | `ga-2`（8px）起步，密集区至少 `ga-1` | 元素间呼吸 |
| 图标按钮 | 允许 `size="small"` | 图标不需要大内边距 |
| 输入行（如 console 输入条） | `px-4 py-4` | 输入框 + 按钮组合需要纵向空间 |

---

## 4. 常见误区（本次项目踩过的坑）

**4.1 用 `size="small"`/`size="x-small"` 塞更多内容**
症状：停止/移除/^C 按钮文字贴顶、chip 文字贴边框。
解法：文字按钮/chip 一律默认密度，`size="small"` 只留给图标按钮。

**4.2 一行塞太多控件，靠减小间距硬排**
症状：autoscroll/清空/导出/停止/移除 + 状态 chip 挤成一团。
解法：**分组**。图标工具一组（`ga-1`），生命周期操作一组（`ga-2`），两组间自然留白；空间不足用 `flex-wrap` 换行。

**4.3 chip 不设内边距**
症状：「N 个运行中」「运行中」「CPU 90%」等 chip label 贴顶。
解法：统一给面板内 chip 加 `padding-block: 4px; min-height: 24px`。

**4.4 计时/状态显示用"当前时钟"而非"结束时间"**
症状：已停止/错误的任务时长还在跳。
解法：数据模型加 `endedAt`，界面用 `endedAt` 冻结已结束任务的显示。

**4.5 只调数字，不删错属性**
症状：改了 padding 但模板里还留着 `size="small"`，视觉没变化。
解法：改样式时**同时检查模板属性**——Vuetify 的 `size`/`density` 优先级高于自定义 CSS，模板里没删干净等于白改。

---

## 5. 本次案例复盘：后台任务面板（BackgroundTasksDialog）

按上述规范重构后的布局结构（`src/main/ui/components/BackgroundTasksDialog.vue`）：

```
┌──────────────────────────────────────────────────────────┐
│ 标题: 后台任务 [运行中 chip]        [移除已结束] [×]       │  ← py-4, chip 有 padding
├──────────────────────────────────────────────────────────┤
│  搜索栏                    [状态筛选]                      │
│  ├─ 任务列表（chip 状态 + CPU/内存/显存 chips）            │  ← chip 默认密度
│  └─ 详情:
│      ├─ 名称 [状态chip] ──  [图标工具组] [文字操作组]      │  ← 分组, pb-3
│      ├─ 控制台输出                                       │
│      └─ [^C][^D] [输入框] [发送]                          │  ← py-4, 文字按钮默认密度
└──────────────────────────────────────────────────────────┘
```

关键取舍：
- **图标工具**（自动滚动/清空/导出）`size="small"` + `ga-1`：紧凑合理
- **文字操作**（停止/强制结束/移除）默认密度 + `ga-2`：不贴顶
- **chip**（状态/统计）统一 `padding-block: 4px; min-height: 24px`

---

## 6. 检查清单（提交 UI 改动前）

- [ ] 文字按钮/chip 是否用了默认密度？（`size="small"` 只留给图标按钮）
- [ ] chip 是否有 `padding-block` / `min-height`，label 不贴边？
- [ ] 一行是否塞太多？是否已按「图标工具 / 文字操作」分组，且 `flex-wrap` 兜底？
- [ ] 区块间距 `py/pb` 是否 ≥ 3，`gap` ≥ 8px？
- [ ] 模板属性（`size`/`density`）是否与 CSS 意图一致，没有残留旧属性？
- [ ] 状态变化（结束/错误/完成）是否有数据支撑（如 `endedAt`），UI 正确表现而非继续滚动？
- [ ] 布局是否符合 apps 能力的排版范式（`v-card-actions` + `ga-2` + `flex-wrap`）？
